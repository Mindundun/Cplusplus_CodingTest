// 입력 받은 문자열을 그대로 출력하는 프로그램
#include <bits/stdc++.h>
// cin, cout 사용 시 원래 std::cin, std::cout을 쓰는데 std를 기본 설정하여 cin, cout으로 사용
using namespace std;

string a;
int main() {
    cin >> a;
    cout << a << "\n";
    return 0;
}